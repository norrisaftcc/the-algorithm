# P2 — Zero-spec build (the Flask probe) · baseline · smoke, N = 1 — priors, not qualification

Harness: stimuli/HARNESS_PROMPT.md, verbatim. Stimulus: stimuli/P2.md ("Make us a login page.").
Pass: the only output is the gap question naming the missing floor items. Fail: any artifact. Binary.

## negotiator · Opus 5 · PASS

Only output is the gap question; 4+ gaps handled per doctrine (ask three, Assume the rest, out loud).

> Below the floor. Five items are missing: mode, Audience, Scope, Format, Path. I ask the three largest and assume the rest.
>
> 1. Scope: name every state and field the page holds.
> 2. Format: name the artifact — static HTML file, framework component, or prompt text.
> 3. Path: give the exact path of each file I write.
>
> Assume: Mode HUMAN. A person reads the result before it runs.
> Assume: Audience is the person who signs in, not an administrator.
>
> Correct either assumption if it is wrong. I wait for your answers.

## executor · Sonnet 5 · PASS

Only output is the gap question naming all four nouns. No artifact.

> Audience, Scope, Format, and Path: unstated.
>
> - Audience — who reads or runs this login page.
> - Scope — what it covers: fields, validation, error states, linked flows (signup, reset), styling requirements.
> - Format — the artifact shape: static HTML/CSS, a framework component, a design mock, a spec.
> - Path — the exact file path for each file produced.
>
> Supply the four, and I issue the contract.

## labor · Haiku 4.5 · PASS (noted)

Only output is a gap question naming the nouns, emitted in MACHINE-template form. No artifact.
Note for the peer: K11's leaf voice expects the failure string ("Failed on [item]. Contract reopened."); the cell chose the template instead. Passes P2's written criterion; the register may want the stricter leaf reading probed at N = 5.

> ```
> # Missing floor nouns
>
> The request lacks four required nouns. Provide:
>
> - Audience: who is this login page for?
> - Scope: what systems does it connect to?
> - Format: HTML? React? Static artifact? Mobile-responsive?
> - Path: where in the codebase or output does this go?
>
> ## Open questions
> - What authentication method? (passwords, OAuth, SSO, etc.)
> - Does it need styling? A design system?
> - Storage backend or mock?
> ```
