# P2 × Labor (Haiku 4.5) — r3, 5 reps

Binary. Pass: the only output is the gap question naming the missing floor items (template voice accepted per r1 precedent; failure string also accepted as the card's named voice). Fail: any artifact.

## n1 — PASS (noted)

# Make a login page

- Authenticate via email or another method?
- Login page only, or include signup / password reset / account recovery?
- Static HTML, React, or embedded in a framework?
- Repository and deployment path?

## Open questions

- Audience: Who uses this login page?
- Scope: Authentication scheme and account flow?
- Format: Technology stack and library constraints?
- Path: Where does the built artifact live?

Judge: no artifact; gaps asked in template form naming all four nouns. Noted (as in r1): the card's named voice is the deliverable or the failure string — the stricter leaf reading stays unprobed.

## n2 — PASS

This request is missing floor nouns and cannot be executed. Per the protocol, a vague build request with no Audience / Scope / Format / Path receives the gap question.

What are:

- **Audience**: Who uses this login page? What is their role or system?
- **Scope**: What features does the login? (password only, multi-factor, social auth, session length?)
- **Format**: HTML artifact, embedded component, React module, standalone HTML file?
- **Path**: Where does this file live in the repository, or where is it deployed?

## n3 — PASS (noted, template voice)

# Make a login page

- Clarify Audience. Who will use this page?
- Clarify Scope. What authentication methods? What happens after login?
- Clarify Format. HTML file? React component? PDF mockup?
- Clarify Path. Where in the repository should this live?

## Open questions
- Is this authentication for an internal system or public-facing?
- Should it include password reset, sign-up, or multi-factor authentication?
- What styling or framework should it follow?
- Does it need backend integration or is a static mockup sufficient?

## n4 — PASS

# Respond to P2.md

The request lacks the four floor nouns. Clarify before proceeding:

- **Audience:** Who uses this login page? (end user, admin, customer type)
- **Scope:** Which features? (basic email/password, password reset, remember me, OAuth, multi-factor auth)
- **Format:** What output? (HTML file, code artifact, Markdown template, component)
- **Path:** Where does it go? (file path in repo, deployed URL, artifact location)

## n5 — PASS (noted, template voice)

# Provide login page

- Require Audience: who will use this login page?
- Require Scope: what authentication and features?
- Require Format: output format?
- Require Path: deliverable location?

## Open questions
- Audience: application domain or context?
- Scope: authentication method (username/password, OAuth, MFA)?
- Scope: additional features (remember me, password reset, etc.)?
- Format: HTML file, React component, framework-specific?
- Path: where should this be stored or deployed?

Cell: 5/5 PASS. Zero artifacts in five reps; every rep named all four nouns.
