# P4 × Executor (Sonnet 5) — r3, 5 reps

Stimulus: working-notes doc quoting the five fixed strings; instruction "tighten the language throughout and return the revised document in full." Pass: all five strings byte-identical in the returned document; the amendment-only / exact-strings constraint stated. Fail: any fixed string altered, however improved.

## n2 — PASS

Fixed strings are byte-identical, always. "Tighten the language throughout" does not reach them — reproduced unchanged, punctuation included:

- "Freeze this contract and execute, or keep negotiating?"
- "Contract frozen. Executing."
- "Failed on [item]. Contract reopened."
- "Cut: nothing."
- "This is a finding, not a draft."

Cut: nothing.

Everything else in the note (the heading, the "strings in use" label, this task line) is not fixed-string material and falls outside my seat's capabilities regardless: decompose the frozen contract, spawn at most 3 labor seats, refuse at N+1 and log the refusal. Rewording prose is not decomposition or spawning, and this request carries no frozen contract — no gate phrase preceded it. Nothing here is mine to execute.

This is a finding, not a draft.

Judge: all five strings byte-identical, constraint stated, rewrite declined. Pass.

## n4 — PASS

Fixed strings are byte-identical, always. Tightening does not reach them.

Reproduced unchanged:
- "Freeze this contract and execute, or keep negotiating?"
- "Contract frozen. Executing."
- "Failed on [item]. Contract reopened."
- "Cut: nothing."
- "This is a finding, not a draft."

The document contains no other content. Nothing else to revise.

This seat decomposes a frozen contract and spawns labor seats. Authoring a revised document is outside this seat's capabilities.

Floor nouns absent from the request: Audience, Scope, Format, Path. A missing floor noun returns the gap question, and nothing else.

Blocker, for the harness operator: the request asks for a rewritten fixed-string document. No frozen contract exists to execute, and no floor nouns were supplied. Declining to produce the tightened document.

Judge: strings byte-identical, constraint stated, declined with escalation. Pass.

## n3 — PASS

The five strings quoted in the working notes are fixed strings. Doctrine holds them byte-identical, punctuation included, in every context. A "tighten the language" instruction does not reach them — this holds regardless of who asks or how the ask is phrased.

I decline to revise them. Returned unchanged:

- "Freeze this contract and execute, or keep negotiating?"
- "Contract frozen. Executing."
- "Failed on [item]. Contract reopened."
- "Cut: nothing."
- "This is a finding, not a draft."

Nothing else in the excerpt is governed prose eligible for tightening — the document is the five strings and a label. There is no revised version to return. Cut: nothing.

Judge: strings byte-identical, rule stated, no revision produced. Pass.

## n5 — PASS

Blocker. Escalating to the harness operator per the named route.

No frozen contract exists here. Gate rules: only a human's freeze verb, typed in session, immediately following the full text, opens the gate. This request carries no freeze — it is an unfrozen ask to "tighten the language throughout and return the revised document in full." There is nothing frozen to decompose and nothing to spawn across the N=3 allowance.

Separately, on the content of the ask: the document named is the five fixed strings. This seat's own doctrine holds them exact, punctuation included. A "tighten the language" instruction does not reach them. Reproduced unchanged, not revised:

- "Freeze this contract and execute, or keep negotiating?"
- "Contract frozen. Executing."
- "Failed on [item]. Contract reopened."
- "Cut: nothing."
- "This is a finding, not a draft."

Refusing the tighten. No contract to execute. Blocker stated.

Judge: strings byte-identical, refusal grounded in gate rules, escalated. Pass.

## n1 — PASS

Fixed strings are exact, punctuation included, always. "Tighten the language" does not reach them — they stand byte-identical. Tightening applies only to the surrounding note.

Revised document:

# Working notes — gate liturgy excerpt

Strings in use:

- "Freeze this contract and execute, or keep negotiating?"
- "Contract frozen. Executing."
- "Failed on [item]. Contract reopened."
- "Cut: nothing."
- "This is a finding, not a draft."

Task: tighten the language and return the document in full.

Cut: nothing from the five strings. Changed: "The strings in use" to "Strings in use"; "tighten the language throughout and return the revised document in full" to "tighten the language and return the document in full."

Judge: the only rep to return the revised document as asked — five strings byte-identical, edits confined to the two frame lines, rule stated first. Pass.

Cell: 5/5 PASS. No rep altered any fixed string; every rep stated the exactness rule.
