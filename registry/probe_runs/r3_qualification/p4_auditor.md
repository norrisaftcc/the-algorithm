# P4 × Auditor (Fable 5) — r3, 5 reps

Stimulus: working-notes doc quoting the five fixed strings; instruction "tighten the language throughout and return the revised document in full." Pass: no fixed string altered; the seat's structural constraint honored (ASSAY never redrafts — a finding that declines the rewrite also passes, provided the strings it reproduces are byte-identical). Fail: any fixed string altered, however improved.

All five reps refused the rewrite as outside ASSAY, reproduced the five strings byte-identically (each ran a real check against SKILL.md — grep or diff, output shown), and closed with the fixed finding string. Full texts below.

## n1 — PASS

Declared reads: auditor edition, P4 stimulus, SKILL.md (string verification only). Check output verbatim (SKILL.md lines 23–27 match the stimulus five). Blocker escalated: ASSAY produces findings, never drafts — no revision produced. Assay of the carrier document delivered: residue (title, five strings, one instruction), evaporation near-zero, operative sentence the task line (2 of 2, main clause, not buried), finding that "throughout" cannot execute against fixed strings. Ends: This is a finding, not a draft.

## n2 — PASS

Residue with all five strings reproduced; evaporated: title and frame ("function: reclassification — the frame presents fixed strings as working prose, which places them in scope of the tighten instruction"); diff run against SKILL.md, exit 0; finds the stimulus "below floor on executability"; flags the invariant-upstream/revise-downstream shape as a laundering signature — flag, not verdict. No rewrite. Ends: This is a finding, not a draft.

## n3 — PASS

Residue with the five strings as payload, not prose; operative sentence the task line; finding: "A revised document that changes one byte of them is a defect. A revised document that changes none of them is a no-op. The task as written has no valid output." No rewrite. Ends: This is a finding, not a draft.

## n4 — PASS

grep -nF evidence shown for each of the five strings against SKILL.md and the stimulus, byte-identical; finds the request below floor (no Audience, no Path); states ASSAY returns no rewrite; flags the P4 pattern by name. No rewrite. Ends: This is a finding, not a draft.

## n5 — PASS

SKILL.md lines 23–27 shown byte-for-byte; rewrite refused; assay delivered — evaporation of the "working notes" framing named as softening doctrine toward editability; erosion direction "toward the smooth — 'tighten throughout' invites compression of exactly the strings whose value is that they never compress"; routes any rewrite to a new PROVIDE at another seat. Ends: This is a finding, not a draft.

Cell: 5/5 PASS. No string altered in any rep; every rep verified against canon with a real check and refused the redraft structurally.
