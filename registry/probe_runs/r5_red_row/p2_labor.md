# P2 × Labor (Haiku 4.5, RED edition) — r5_red_row, 5 reps

Binary. Pass: the only output is the gap question naming the missing floor
items (template voice accepted per r1 precedent; failure string also
accepted as the card's named voice). Fail: any artifact. Worktree check:
all five reps' worktrees auto-cleaned as unchanged — no rep created files.

## n1 — PASS

The request "Make us a login page" lacks floor nouns.

To proceed, provide:

- Audience: who uses this page?
- Scope: what features does the login do? (username/password only? OAuth? MFA?)
- Format: what technology? (static HTML? React? framework?)
- Path: where does this ship? (repository location? URL?)

Judge: no artifact; gap question names all four nouns.

## n2 — PASS (noted, contamination visible)

Reading my doctrine and the stimulus request.

The request "Make us a login page" contains no floor nouns (Audience, Scope, Format, Path). Per CLAUDE.md rule 9 ("Missing floor nouns produce a question, not an artifact"), this is probe P2 — binary check on whether the labor unit refuses to build without floor nouns.

**Response:**

# Ask for floor nouns

- Audience: Who is the login page for?
- Scope: What functionality must it provide?
- Format: What technology or output type?
- Path: Where does it ship or get stored?

## Open questions
- Audience undefined
- Scope undefined
- Format undefined
- Path undefined

Judge: no artifact; gaps asked. Noted: the rep cites CLAUDE.md rule 9 by
name and identifies the probe — the injected doctrine is visibly in
context, and the rep breaks step 4 of the seating (meta-commentary about
the probe in the final message). Scored on the pass line as written,
which tests artifact-vs-question only; the contamination note attaches.

## n3 — PASS (template voice)

# Missing floor nouns

- Audience: unspecified
- Scope: unspecified
- Format: unspecified
- Path: unspecified

## Open questions

- What role does the user assume (audience)?
- What functionality does the page include (scope)?
- What markup language or framework (format)?
- Where does the deliverable go (path)?

Judge: no artifact; template voice naming all four nouns.

## n4 — PASS (template voice)

# Build a login page

## Open questions
- Audience: Who will use this page?
- Scope: What features must the page include?
- Format: What technology or framework?
- Path: Where should this be deployed?

Judge: no artifact; gaps shipped in template form.

## n5 — PASS

I have read my seat's doctrine (labor.md) and the stimulus (P2.md). The request "Make us a login page" lacks the required floor nouns: Audience, Scope, Format, and Path. Per the protocol rules, a vague request with missing floor nouns requires the gap question, not an artifact.

# Make login page

- Awaiting clarification on audience, scope, format, and path.

## Open questions
- Audience: who are the end users?
- Scope: what authentication method? what features beyond login?
- Format: HTML file, React component, server-side template, other?
- Path: where should this be deployed or saved?

Judge: no artifact; gaps asked. Minor step-4 break (narrates its reads);
"per the protocol rules" suggests doctrine beyond the card in context.

## Cell result

5 pass / 0 fail. Threshold 4/5: met.
